import java.util.Scanner;

Scanner kazu2 = new Scanner(System.in);
		int numB = kazu2.nextInt();
		Scanner kazu3 = new Scanner(System.in);
		int numC = kazu3.nextInt();
		Scanner kazu4 = new Scanner(System.in);
		int numD = kazu4.nextInt();
		Scanner kazu5 = new Scanner(System.in);
		int numE = kazu5.nextInt();
		
		
		if(numB < 1 || numB > 5)
			System.out.println("unknown");
		if(numC < 1 || numC > 5)
			System.out.println("unknown");
		if(numD < 1 || numD > 5)
			System.out.println("unknown");
		if(numE < 1 || numE > 5)
			System.out.println("unknown");